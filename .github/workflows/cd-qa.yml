name: Desplegar a QA

# develop → https://benjaminvalenzuela.github.io/nuestro-espacio/qa/
on:
  push:
    branches: [develop]
  workflow_dispatch:

concurrency:
  group: despliegue-qa
  cancel-in-progress: false   # nunca cancelar un despliegue a medias

permissions:
  contents: write             # escribe en la rama gh-pages

jobs:
  desplegar:
    name: Compilar y publicar en /qa
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: qa

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Configuración del entorno QA
        # Se escribe un .env en vez de exportar variables: Vite lee los .env de
        # forma predecible, y así el build es idéntico al que se hace en local.
        #
        # Estos valores son IDENTIFICADORES PÚBLICOS, no secretos: acaban en el
        # bundle igualmente. Lo que protege los datos son las Security Rules,
        # el registro cerrado, las restricciones de referrer y App Check.
        run: |
          cat > frontend/.env <<'EOF'
          PUBLIC_ENTORNO=qa
          PUBLIC_FIREBASE_API_KEY=${{ vars.FIREBASE_API_KEY }}
          PUBLIC_FIREBASE_AUTH_DOMAIN=${{ vars.FIREBASE_AUTH_DOMAIN }}
          PUBLIC_FIREBASE_PROJECT_ID=${{ vars.FIREBASE_PROJECT_ID }}
          PUBLIC_FIREBASE_APP_ID=${{ vars.FIREBASE_APP_ID }}
          PUBLIC_FIREBASE_DATABASE_URL=${{ vars.FIREBASE_DATABASE_URL }}
          PUBLIC_PAREJA_ID=${{ vars.PAREJA_ID }}
          PUBLIC_RECAPTCHA_SITE_KEY=${{ vars.RECAPTCHA_SITE_KEY }}
          EOF
          echo "Variables escritas (los valores no se imprimen)."

      - name: Compilar
        env:
          # GitHub Pages sirve el sitio bajo una subruta. Astro necesita saberla
          # en compilación para reescribir enlaces y assets.
          PUBLIC_BASE_PATH: /nuestro-espacio/qa/
          PUBLIC_SITE_URL: https://benjaminvalenzuela.github.io
        run: npm run build

      - name: Publicar en gh-pages/qa
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./frontend/dist
          destination_dir: qa
          # ═══════════════════════════════════════════════════════════════
          #  keep_files: true — y NO es por comodidad.
          #
          #  Con false, cada despliegue BORRA los assets del anterior. Los
          #  nombres llevan hash, así que los nuevos son otros archivos. El
          #  problema es que GitHub Pages cachea el HTML unos minutos en su
          #  borde, y el navegador otro tanto: durante esa ventana, quien
          #  recibe el HTML viejo pide unos assets que ya no existen y la
          #  aplicación se rompe con una pantalla de "Cargando" eterna y
          #  cuatro 404 en la consola.
          #
          #  Se comprobó en QA: el HTML servido pedía identidad.CEQZ9Tyn.js
          #  mientras el despliegue había dejado identidad.CI6SA5UM.js.
          #
          #  Conservando los archivos, las dos versiones conviven y la
          #  transición es invisible. La basura acumulada se limpia cuando
          #  haga falta con el workflow "Reconstruir gh-pages".
          # ═══════════════════════════════════════════════════════════════
          keep_files: true
          commit_message: "qa: ${{ github.event.head_commit.message }}"

      - name: Resumen
        run: |
          echo "### QA desplegado ✅" >> $GITHUB_STEP_SUMMARY
          echo "https://benjaminvalenzuela.github.io/nuestro-espacio/qa/" >> $GITHUB_STEP_SUMMARY
